import{_ as s,c as n,a,e as r,f as d,b as t,r as i,d as l}from"./app-XtQ0myIh.js";const c={};function h(p,e){const o=i("SkillDownload");return l(),n("div",null,[e[0]||(e[0]=a("h1",{id:"agent-skill",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#agent-skill"},[a("span",null,"Agent Skill")])],-1)),e[1]||(e[1]=a("p",null,[t("A packaged "),a("a",{href:"https://agentskills.io/specification",target:"_blank",rel:"noopener noreferrer"},"Agent Skill"),t(" that teaches a coding assistant to write Volga correctly — the same material as this site, reorganised for a model rather than a reader.")],-1)),r(o,{href:"/volga-skill.zip",label:"Download volga-skill.zip",note:"~50 KiB · MIT · tracks volga 0.12.0"}),e[2]||(e[2]=d(`<p>Source: <a href="https://github.com/RomanEmreis/volga-docs/tree/main/skill/volga" target="_blank" rel="noopener noreferrer"><code>skill/volga</code></a>.</p><h2 id="why-you-might-want-it" tabindex="-1"><a class="header-anchor" href="#why-you-might-want-it"><span>Why you might want it</span></a></h2><p>Assistants are confidently wrong about Volga, and for a specific reason: the 0.9 line changed security defaults and removed a family of helper methods, 0.10.0 rebuilt how requests reach middleware and how route groups apply what they hold, 0.10.1 changed what a rejected token is answered with, and 0.11.0 added synchronous handlers, catch-all routes and a shutdown timeout, 0.11.1 gave a route group a fallback of its own, and 0.12.0 turned a handler&#39;s <code>Err</code> into an error for <code>map_err</code> rather than a second response, while most Volga code a model has seen predates all of it. So the failure mode is not a forgotten method name. It is an assistant writing <code>ok!(&quot;hi&quot;, [(&quot;x-key&quot;, &quot;v&quot;)])</code> with a comma instead of a semicolon, returning <code>Err(&quot;not found&quot;.to_string())</code> and getting a <code>500</code>, reaching for <code>with_default_cors()</code>, calling <code>use_cors()</code> on an app that never configured CORS, mapping static files with <code>map_static_assets()</code>, or shipping a bearer-auth setup that answers <code>400</code> to every request once it sits behind a TLS-terminating proxy.</p><p>The skill front-loads exactly those traps, then routes to detail on demand.</p><h2 id="what-is-in-it" tabindex="-1"><a class="header-anchor" href="#what-is-in-it"><span>What is in it</span></a></h2><table><thead><tr><th>File</th><th>Covers</th></tr></thead><tbody><tr><td><code>SKILL.md</code></td><td>Establishing the version and features, the non-negotiables, a minimal app, routing</td></tr><tr><td><code>references/routing.md</code></td><td>Routes, groups, path/query params, JSON, forms, files, multipart, headers, cookies</td></tr><tr><td><code>references/responses.md</code></td><td>Response macros, <code>IntoResponse</code>, status codes, streaming, errors and <code>IntoError</code>, Problem Details</td></tr><tr><td><code>references/middleware.md</code></td><td><code>with</code> / <code>wrap</code> / <code>attach</code> / <code>filter</code> / <code>tap_req</code> / <code>map_ok</code>, CORS, compression, static files, rate limiting</td></tr><tr><td><code>references/di-config.md</code></td><td>DI lifetimes, <code>Inject</code>, factories, configuration files, hot reload</td></tr><tr><td><code>references/security.md</code></td><td>Basic auth, JWT, authorizers, OAuth 2.1 / OIDC, the client crate, TLS, HSTS</td></tr><tr><td><code>references/realtime.md</code></td><td>WebSockets, WebSocket-over-HTTP/2, Server-Sent Events</td></tr><tr><td><code>references/operations.md</code></td><td>Feature flags, graceful shutdown, cancellation, tracing, OpenAPI and hand-written schemas, <code>TestServer</code></td></tr><tr><td><code>references/migration.md</code></td><td>Symptom → cause, and the 0.11.x → 0.12.0, 0.10.x → 0.11.0, 0.10.0 → 0.10.1, 0.9.x → 0.10.x and 0.8.x → 0.9.x upgrade paths</td></tr></tbody></table><p><code>SKILL.md</code> stays short on purpose: an entrypoint an agent always reads, and eight references it loads only when the task needs one.</p><h2 id="install" tabindex="-1"><a class="header-anchor" href="#install"><span>Install</span></a></h2><p>The SKILL.md format is a shared standard, so installation is the same everywhere: unzip and <strong>copy the <code>volga/</code> directory into the tool&#39;s skills folder</strong>, keeping the folder name — it has to match the <code>name</code> in the frontmatter.</p><table><thead><tr><th>Tool</th><th>Personal</th><th>Per project</th></tr></thead><tbody><tr><td>Claude Code</td><td><code>~/.claude/skills/volga/</code></td><td><code>.claude/skills/volga/</code></td></tr><tr><td>opencode</td><td><code>~/.config/opencode/skills/volga/</code></td><td><code>.opencode/skills/volga/</code></td></tr><tr><td>Codex CLI</td><td><code>~/.codex/skills/volga/</code></td><td><code>.codex/skills/volga/</code></td></tr></tbody></table><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">unzip</span> volga-skill.zip</span>
<span class="line"><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> ~/.claude/skills <span class="token operator">&amp;&amp;</span> <span class="token function">cp</span> <span class="token parameter variable">-r</span> volga ~/.claude/skills/</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Restart the assistant afterwards — skills are discovered at startup.</p><p>opencode also reads <code>.claude/skills/</code> and <code>.agents/skills/</code>, so a single copy inside a project can serve more than one tool.</p><h3 id="anything-else" tabindex="-1"><a class="header-anchor" href="#anything-else"><span>Anything else</span></a></h3><p>Any assistant that can read a file will do. Point it at <code>SKILL.md</code> and let it follow the links, or add a line to the project&#39;s <code>AGENTS.md</code>:</p><div class="language-markdown line-numbers-mode" data-highlighter="prismjs" data-ext="md"><pre><code class="language-markdown"><span class="line">For Rust web work with the <span class="token code-snippet code keyword">\`volga\`</span> crate, read</span>
<span class="line"><span class="token code-snippet code keyword">\`.agents/skills/volga/SKILL.md\`</span> and the reference file it routes you to.</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-it-front-loads" tabindex="-1"><a class="header-anchor" href="#what-it-front-loads"><span>What it front-loads</span></a></h2><p>The whole point of shipping a skill rather than a prose summary is the list of things a model gets wrong on its own. A sample of what <code>SKILL.md</code> puts before anything else:</p><ul><li><strong>Custom headers come after a semicolon.</strong> <code>ok!(&quot;Hello&quot;; [(&quot;x-key&quot;, &quot;v&quot;)])</code>. A comma matches the <code>format!</code> arm and fails to compile.</li><li><strong><code>with_*</code> consumes the <code>App</code>, <code>map_*</code> borrows it.</strong> Configuration is chained first; routing needs <code>let mut app</code>.</li><li><strong><code>require_https</code> and <code>strip_token_from_request</code> default to on</strong> for bearer authentication — the reason a working local setup returns <code>400</code> behind a reverse proxy.</li><li><strong><code>with_aud</code> makes <code>aud</code> a required claim</strong> unless <code>without_strict_aud()</code> says otherwise.</li><li><strong><code>with_default_cors()</code> and <code>with_default_tracing()</code> are gone</strong>, and the on/off builders (<code>with_credentials</code>, <code>with_preload</code>, …) no longer take a <code>bool</code>.</li><li><strong><code>Path&lt;T&gt;</code> is a tuple, <code>NamedPath&lt;T&gt;</code> is the named struct</strong>, and neither may be mixed with positional path parameters in one handler.</li><li><strong><code>full</code> is not everything</strong> — <code>#[derive(Claims)]</code>, <code>#[http_header]</code>, development certificates and <code>TestServer</code> each need a feature <code>full</code> does not include.</li><li><strong>A rejected bearer token is <code>401</code>, not <code>403</code></strong> since 0.10.1 — <code>403</code> is left to a valid token that lacks the role or permission a route asks for.</li><li><strong>An unresolvable DI graph stops the app at startup</strong> since 0.10.1, so a hand-written <code>impl Inject</code> should declare what it resolves.</li><li><strong>A synchronous handler runs on the runtime worker</strong> — blocking work goes in <code>blocking</code>, not in a plain <code>fn</code> handler.</li><li><strong>A catch-all value is not a safe file path</strong>: <code>{*path}</code> arrives unnormalized, <code>..</code> included.</li><li><strong>Endless responses end on the shutdown signal</strong>, via the <code>ShutdownHandle</code> extractor, instead of being closed at the shutdown timeout.</li><li><strong>The SPA shell answers <code>GET</code> and <code>HEAD</code> alone</strong> since 0.11.1, and an API sharing the server gives its group a <code>map_fallback</code> of its own.</li><li><strong>A handler&#39;s <code>Err</code> is an error</strong> since 0.12.0: it reaches <code>map_err</code>, a bare string answers <code>500</code>, and an error type implements <code>IntoError</code> — never <code>From&lt;T&gt; for Error</code>.</li></ul><h2 id="the-code-in-it-compiles" tabindex="-1"><a class="header-anchor" href="#the-code-in-it-compiles"><span>The code in it compiles</span></a></h2><p>The Rust in the skill is compiled against the published <code>volga</code> crates in this repository&#39;s CI, on the same job that checks the snippets on this site — so what an assistant copies out of it builds. Blocks that show a shape rather than a program (a signature, a trait impl with elided bodies) opt out explicitly.</p><p>Run the same check yourself after editing:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">python3 ci/check-snippets.py --docs-dir skill --default-mode compile-fragment --default-features <span class="token string">&quot;full auth-full macros dev-cert test&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="version" tabindex="-1"><a class="header-anchor" href="#version"><span>Version</span></a></h2><p>The skill tracks volga <strong>0.12.0</strong> on MSRV <strong>1.90</strong>. The frontmatter records both, so an assistant can tell whether the skill matches the crate in front of it:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">metadata</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">volga-version</span><span class="token punctuation">:</span> <span class="token string">&quot;0.12.0&quot;</span></span>
<span class="line">  <span class="token key atrule">msrv</span><span class="token punctuation">:</span> <span class="token string">&quot;1.90&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,26))])}const u=s(c,[["render",h]]),g=JSON.parse('{"path":"/en/agent-skill.html","title":"Agent Skill","lang":"en-US","frontmatter":{},"git":{"updatedTime":1790425107000,"contributors":[{"name":"RomanEmreis","username":"RomanEmreis","email":"39233213+RomanEmreis@users.noreply.github.com","commits":6,"url":"https://github.com/RomanEmreis"},{"name":"Claude Opus 5","username":"","email":"noreply@anthropic.com","commits":6},{"name":"Claude","username":"Claude","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude"},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"7f1c344acb2af155e575dc17cde5910131359338","time":1790425107000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Document volga 0.12.0 and 0.11.2: errors from handlers, filter verdicts, OpenAPI (#60)","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]},{"hash":"01b9e0c62fb643147fdafad38a6d5841ea9d586d","time":1789881833000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Document volga 0.11.1: group fallbacks and the static-file shell (#59)","coAuthors":[{"name":"Claude Opus 5","email":"noreply@anthropic.com"}]},{"hash":"24c96feb47a8decb8483a0e9e93a10b225304752","time":1789800990000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Document volga 0.11.0: synchronous handlers, catch-all routes, shutdown timeout (#58)","coAuthors":[{"name":"Claude Opus 5","email":"noreply@anthropic.com"}]},{"hash":"5444fa3528e77ec457f471a1f6925dede660575a","time":1789368324000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Document volga 0.10.1 changes: auth status codes, DI validation, and fixes (#57)","coAuthors":[{"name":"Claude Opus 5","email":"noreply@anthropic.com"},{"name":"Claude Opus 5","email":"noreply@anthropic.com"},{"name":"Claude","email":"noreply@anthropic.com"}]},{"hash":"8963686f5ff6dfa158e184f9031a26dc9e0f404f","time":1788887211000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Cover 0.9.10 through 0.10.0, where most of it is behaviour rather than API (#56)","coAuthors":[{"name":"Claude Opus 5","email":"noreply@anthropic.com"}]},{"hash":"fec92f1f4cd6b7997845ee22c27481b3970816e0","time":1787467624000,"email":"39233213+RomanEmreis@users.noreply.github.com","author":"Roman","message":"Ship a volga Agent Skill, and fix the snippets the checker was not watching (#54)","coAuthors":[{"name":"Claude Opus 5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"en/agent-skill.md"}');export{u as comp,g as data};
